package com.saucypixelstudios.trixeltechnician.trixels;

import java.awt.Graphics;
import java.awt.Point;

public class Trixel {
	private double scale;
	private int direction;
	private int x;
	private int y;
	private Point[] xPoints;
	private Point[] yPoints;

	public Trixel(double scale, int direction, int x, int y){
		this.scale = scale;
		this.direction = direction;
		this.x = x;
		this.y = y;
		vertices = new Point[3];
		generatePoints();
	}
	
	public void generatePoints(){
		if(direction == TrixelDirectionMap.RIGHT){
			vertices[0] = new Point(x, y);
			System.out.println("Vertex 0: " + vertices[0].getX() + ", " + vertices[0].getY());
			vertices[1] = new Point(x, (int) (y + TrixelBaseSideLength.LENGTH * scale));
			System.out.println("Vertex 1: " + vertices[1].getX() + ", " + vertices[1].getY());
			vertices[2] = new Point(x + TrixelBaseSideLength.getMedian(), (int) (y + TrixelBaseSideLength.LENGTH * scale * 0.5));
			System.out.println("Vertex 2: " + vertices[2].getX() + ", " + vertices[2].getY());
		}
	}

	public void drawTrixel(Graphics g) {
		
		g.draw
		
	}
}
